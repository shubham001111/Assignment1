#include<stdio.h>
#include<string.h>
int main()
{
char ch[10];
int count=0;
printf("Enter number : ");
scanf("%s",ch);
char ch1='.';
for(int i=0;i<strlen(ch);i++)
{
if(ch[i]==ch1)
{
if(ch[i+1]!='\0')
{count++;
printf("Valid\n");
break;}
}
}
if(count==0)
printf("Invalid\n");
return 0;
}
